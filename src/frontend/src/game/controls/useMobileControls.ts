import { useEffect } from 'react';

export function useMobileControls() {
  useEffect(() => {
    // Mobile controls are handled by HUD touch components
    // This hook exists for symmetry and future mobile-specific logic
  }, []);
}
